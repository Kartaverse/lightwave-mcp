{
  "name": "connect_modeler",
  "description": "Connect to a LightWave Modeler instance at the specified address and port. Returns a connection ID that can be used for subsequent commands.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "address": {
        "type": "string",
        "description": "The IP address or hostname of the LightWave Modeler instance (e.g., 'localhost' or '127.0.0.1')",
        "default": "localhost"
      },
      "port": {
        "type": "integer",
        "description": "The command port number for the LightWave Modeler instance"
      }
    },
    "required": ["port"]
  }
}
